#include<iostream>
#include<iomanip>
using namespace std;

int main (){
    int num = 2;
    double sum;
    while(num != 222){
        sum = sum + (1/num);
        num += 2;
    }
    cout << fixed << setprecision(10) << sum;

}
